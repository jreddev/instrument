import AppBar from '@mui/material/AppBar';
import Toolbar from '@mui/material/Toolbar';

function Footer() {

    return (
        <AppBar id="footer" position="relative" style={{ top: 'auto' }}>
            <Toolbar>
            </Toolbar>
        </AppBar>
    );
}

export default Footer;
